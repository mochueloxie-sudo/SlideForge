# Fixtures 索引

`examples/fixtures/` 下的 `*_scenes.json` 供 Agent 抄结构或本地 smoke，**不是**页数/变体上限。

| 文件 | 说明 |
|------|------|
| `heytea_office_minimal_4p_scenes.json` | 喜茶 `heytea`：**4 页**短示例（cover + 2×content + summary） |
| `heytea_office_scenes.json` | **兼容别名** → 同上（symlink） |
| `four_new_variants_scenes.json` | compare / process_flow / architecture_stack / funnel |
| `verify_notebook_shell_scenes.json` | notebook-tabs + `_content_shell` 校验 |
| `tencent_intro_scenes_fixture.json` | 长稿 intro 片段 |
