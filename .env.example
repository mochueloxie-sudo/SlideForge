# SlideForge v4.0 — Agent-first skill, no external LLM required.
#
# 本文件**不再**包含任何 LLM 凭证（v3 的 MINIMAX_* / LLM_* 已废弃）。
# 内容理解、scenes.json 产出由宿主 Agent 在对话内完成。
# 本 Skill 只在「飞书来源 / 飞书交付」场景需要外部凭证。

# ─── 飞书 Open API（仅在 source 是飞书链接、或 channel=feishu 时需要）────────
# 创建方式：https://open.feishu.cn/app → 创建企业自建应用 → 获取 App ID / Secret
# 所需权限：docx:document（读取文档）、drive:drive（上传文件，channel=feishu 时）
FEISHU_APP_ID=cli_xxxxxxxxxxxxxxxx
FEISHU_APP_SECRET=your_app_secret_here

# ─── 可选配置 ────────────────────────────────────────────────────────────────────
# 手动指定 lark-cli 路径（通常不需要，Step 7 会自动查找）
# LARK_CLI_PATH=/usr/local/bin/lark-cli

# Chrome 可执行文件路径（Puppeteer 自动检测，通常不需要指定）
# CHROME_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome
